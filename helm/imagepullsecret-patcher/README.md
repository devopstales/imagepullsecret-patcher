# imagepullsecret-patcher

![Version: 0.1.0](https://img.shields.io/badge/Version-0.1.0-informational?style=flat-square) ![Type: application](https://img.shields.io/badge/Type-application-informational?style=flat-square) ![AppVersion: 0.1.0](https://img.shields.io/badge/AppVersion-0.1.0-informational?style=flat-square)

Patch ImagePullSecrets to ServiceAccounts cluster-wide

**Homepage:** <https://github.com/devopstales/imagepullsecret-patcher>

## Maintainers

| Name | Email | Url |
| ---- | ------ | --- |
| Páldi Balázs | <devopstales2@gmail.com> |  |

## Values

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| affinity | object | `{}` | Affinity rules for controller Pod. |
| force | bool | `true` | If true, overwrite `imagePullSecrets` even if they already match. |
| image | object | `{"pullPolicy":"IfNotPresent","repository":"ghcr.io/your-org/imagepullsecret-patcher","tag":"latest"}` | Container image registry, name, and tag |
| image.pullPolicy | string | `"IfNotPresent"` | Image pull policy |
| image.repository | string | `"ghcr.io/your-org/imagepullsecret-patcher"` | Repository path (e.g. ghcr.io/your-org/imagepullsecret-patcher) |
| image.tag | string | `"latest"` | Image tag to use (e.g. v0.1.0, latest) |
| imagePullSecrets | list | `[]` | Image pull secrets for the controller Pod (if using private registry for the controller image) |
| loopInterval | int | `30` | Interval (in seconds) between reconciliation loops (ignored if `runOnce: true`). |
| managedOnly | bool | `false` | If true, only patch ServiceAccounts that already contain at least one of the `registrySecretNames`. |
| nodeSelector | object | `{}` | Node selector for controller Pod placement. |
| patchAllServiceAccounts | bool | `false` | If true, patch **all** ServiceAccounts in every namespace. If false, only patch ServiceAccounts listed in `serviceAccounts` (not yet implemented) or `"default"`. ⚠️ Setting this to `true` may impact many ServiceAccounts. |
| registrySecretNames | string | `"docker-registry"` | Comma-separated list of Kubernetes secret names to inject as imagePullSecrets into ServiceAccounts These secrets must already exist in each target namespace (e.g., replicated via mittwald/kubernetes-replicator). |
| resources | object | `{}` (no limits) | Resource requests and limits for the controller Pod. |
| runOnce | bool | `false` | Run the patcher once and exit (useful for CronJob mode). |
| tolerations | list | `[]` | Tolerations for controller Pod. |

----------------------------------------------
Autogenerated from chart metadata using [helm-docs v1.14.2](https://github.com/norwoodj/helm-docs/releases/v1.14.2)
